name: Optimize SVGs

on:
  schedule:
    - cron: "0 0 * * 1"
  push:
    branches:
      - develop
    paths:
      - .github/workflows/optimize_svgs.yml
  workflow_dispatch:

jobs:
  optimize-svgs:
    runs-on: ubuntu-latest
    if: github.event.repository.fork == false
    permissions:
      pull-requests: write
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Install SVGO
        run: npm install -g svgo
      - name: Optimize SVGs
        id: svgo
        run: |
          # Run SVGO optimization multiple times to ensure all optimizations are applied.
          for i in {1..5}; do
            svgo ./svgs
          done
      - name: Create Pull Request
        id: create-pr
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: |
            Update SVGs

            _Auto-generated by `Optimize SVGs` Github workflow._
          title: Update SVGs
          body: _Auto-generated by `Optimize SVGs` Github workflow._
          branch: actions/optimize-svgs
          delete-branch: true
