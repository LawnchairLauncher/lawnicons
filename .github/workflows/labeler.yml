name: "Label PRs"

on:
  pull_request_target:
    types: [ opened, synchronize, reopened ]

jobs:
  label-prs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/labeler@v6
        with:
          sync-labels: true